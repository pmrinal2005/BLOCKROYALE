{
  "name": "blockroyale-io",
  "version": "1.0.0",
  "type": "module",
  "description": "BlockRoyale.io — a lightweight 3D voxel party battle-royale (Three.js, static, Vercel-ready).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "three": "^0.160.1"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "vite": "^5.4.10"
  }
}
